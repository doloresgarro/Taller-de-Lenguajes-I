#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    printf("Ingrese dos ints\n");
    scanf("%d;%d", &a, &b);
    printf("%d y %d",a,b);

    //fscanf --> se utiliza cuando se como esta armado el archivo

    return 0;
}
