#include <stdio.h>
#include <stdlib.h>

int main (int argc, char * argv[]) {

    // imprime carpetas hasta llegar donde tengo el ejercicio guardado
    printf("\nargc = %d",argc);  //recibe uno por defecto
    printf("\nargv[0] => %s",argv[0]);

    return 0;
}
